/* ---------------------------------------------------------------
   MEMORIES BACKEND  (edit the line below, then save)

   Paste the Google Apps Script Web app URL between the quotes.
   Setup steps are in READ-ME-FIRST.txt.

   Leave it empty and the guestbook falls back to saving
   entries only in each visitor's own browser.
--------------------------------------------------------------- */

var MEMORIES_URL = "";
